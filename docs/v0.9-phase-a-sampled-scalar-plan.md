# v0.9 Phase A Plan - Sampled Small-Workload Scalar Strategy

## Goal

Ensure sparse sampled-count workloads choose the low-overhead scalar reducer path by default for small workloads.

## Scope

1. Keep contracts additive with deterministic typed output.
2. Do not change public `Estimator.sampled_expectation_from_counts/2` signatures.
3. Improve strategy selection only inside sampled reducer internals.

## Deliverables

1. Add workload-aware strategy selection (`unit_count x entry_count`) for sampled reductions.
2. Add explicit strategy mode knobs:
   - `:sampled_parallel_mode` (`:auto | :force_parallel | :force_scalar`)
   - `:sampled_parallel_min_work`
3. Preserve deterministic reducer ordering and typed errors.
4. Add guard coverage:
   - `test/nx_quantum/sampled_small_workload_strategy_guard_test.exs`

## Strategic Refactoring Pass (DDD/SOLID/Hexagonal)

1. Keep strategy policy isolated in sampled execution strategy module.
2. Keep parser/reducer/cache modules focused on single responsibility.
3. Keep application APIs unchanged; no adapter leakage into domain/application contracts.

## Status

1. `[x]` Implemented.
2. `[x]` Guard tests added and passing.
