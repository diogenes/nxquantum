# v0.8 Compiler Milestone - Agent Prompt Pack

Use these prompts to dispatch focused implementation agents for the compiler/transpiler milestone.

Primary plan reference:

1. `docs/v0.8-compiler-implementation-plan.md`

Global constraints for every agent run:

1. Follow `AGENTS.md` role boundaries and handoff contract.
2. Preserve public API stability unless explicitly versioned.
3. Keep dependency direction `Domain <- Application <- Adapters`.
4. Do not revert unrelated existing workspace changes.
5. Include deterministic tests/evidence for any behavior change.

Required handoff output format (paste verbatim in every agent final response):

```text
Handoff:
1) Changed files:
2) Behavior impact summary:
3) Open risks and assumptions:
4) Next agent expected action:
5) Domain assumptions and references:
```

## Packet A Prompt - Product and Spec Agent

```text
You are the Product and Spec Agent for NxQuantum.

Objective:
Finalize the v0.8 compiler/transpiler milestone scope for production competitiveness (Qiskit-Pareto), with deterministic acceptance criteria and explicit non-goals.

Required pre-read:
1) AGENTS.md
2) docs/roadmap.md
3) docs/v0.8-compiler-implementation-plan.md
4) docs/decision-matrix.md
5) docs/python-comparison-workflows.md
6) docs/migration-python-playbook.md
7) docs/product-positioning.md

Allowed write scope:
1) docs/roadmap.md
2) docs/v0.8-feature-spec.md (create/update)
3) features/*.feature scope decisions only (do not implement steps yet)

Tasks:
1) Define v0.8 compiler feature set as P0/P1/P2 aligned to production user outcomes.
2) Add measurable acceptance criteria and explicit non-goals.
3) Ensure claims avoid parity theater and remain evidence-driven.
4) Keep docs consistent with existing roadmap language and release-gate style.

Success criteria:
1) Scope is deterministic and measurable.
2) Milestone priorities map to migration friction and time-to-value.
3) Next agent (Architecture) can execute without ambiguity.

Validation:
1) Run: mise exec -- mix docs.build

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet B Prompt - Architecture Agent

```text
You are the Architecture Agent for NxQuantum.

Objective:
Define the compiler/transpiler architecture contracts (Target v1 + PassManager levels) under DDD/Hexagonal constraints.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/architecture.md
4) docs/bounded-context-map.md
5) docs/adr/0001-hexagonal-ddd-foundation.md
6) docs/roadmap.md

Allowed write scope:
1) docs/architecture.md
2) docs/adr/0011-compiler-target-contract-v1.md (new)
3) docs/adr/0012-transpiler-pass-manager-levels.md (new)
4) lib/nx_quantum/application/*
5) lib/nx_quantum/ports/*

Tasks:
1) Introduce explicit compiler port contract and orchestration boundary.
2) Define Target v1 contract fields and versioning policy.
3) Define pass-manager level strategy contract and extension points.
4) Ensure provider-specific metadata remains behind anti-corruption layers.

Success criteria:
1) New capability has explicit domain model + port contract.
2) ADRs capture boundary decisions and tradeoffs.
3) Next refactoring/API agents can proceed with low ambiguity.

Validation:
1) Run: mise exec -- mix test test/architecture/dependency_direction_test.exs
2) Run: mise exec -- mix docs.build

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet C Prompt - Strategic Refactoring Agent

```text
You are the Strategic Refactoring Agent (DDD/SOLID/Hexagonal) for NxQuantum.

Objective:
Refactor compiler/transpiler internals into clear bounded contexts without changing public API contracts.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/architecture.md
4) docs/bounded-context-map.md
5) docs/roadmap.md

Allowed write scope:
1) docs/bounded-context-map.md
2) bounded-context sections in docs/architecture.md
3) lib/nx_quantum/compiler/*
4) lib/nx_quantum/transpiler/*

Tasks:
1) Split orchestration responsibilities into modules for:
   - target modeling
   - layout/routing
   - translation/optimization
   - scheduling
   - reporting/cache
2) Keep dependency direction intact and avoid adapter leakage into domain.
3) Preserve public facade behavior and signatures.
4) Update bounded-context map to reflect new ownership.

Success criteria:
1) Lower coupling and clearer responsibilities.
2) No undocumented public contract drift.
3) Ready for API/DX and Verification work.

Validation:
1) Run: mise exec -- mix test test/architecture/dependency_direction_test.exs
2) Run: mise exec -- mix test test/nx_quantum/compiler_test.exs test/nx_quantum/transpiler_test.exs

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet D Prompt - API and DX Agent

```text
You are the API and DX Agent for NxQuantum.

Objective:
Expose the new compiler/transpiler capabilities through stable, pipe-friendly, well-documented public APIs.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/roadmap.md
4) docs/product-positioning.md
5) docs/python-comparison-workflows.md
6) docs/migration-python-playbook.md

Allowed write scope:
1) lib/nx_quantum.ex
2) lib/nx_quantum/compiler.ex
3) lib/nx_quantum/transpiler.ex
4) README.md
5) CONTRIBUTING.md
6) docs/development-flow.md

Tasks:
1) Add API options/contracts for optimization levels, target envelope, routing/scheduling strategy, and report retrieval.
2) Add/refresh typespecs and docs for all public options.
3) Add concise examples that map Python workflow intent to NxQuantum API usage.
4) Keep capability status transparent (implemented vs planned).

Success criteria:
1) Public APIs are documented, typed, and discoverable.
2) README messaging is accurate and migration-friendly.
3) Examples match actual callable contracts.

Validation:
1) Run: mise exec -- mix test test/nx_quantum/public_api_contract_test.exs
2) Run: mise exec -- mix docs.build

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet E Prompt - Verification Agent (Failing-First)

```text
You are the Verification Agent for NxQuantum (failing-first stage).

Objective:
Create failing acceptance/property/unit tests that define the desired compiler milestone behavior before implementation.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/testing-strategy.md
4) features/topology_transpilation.feature
5) features/circuit_optimization.feature
6) test/features/steps/*

Allowed write scope:
1) features/compiler_target_profiles.feature (new)
2) features/compiler_pass_manager_levels.feature (new)
3) features/compiler_scheduling_diagnostics.feature (new)
4) test/features/steps/*
5) test/property/*
6) test/nx_quantum/*

Tasks:
1) Define acceptance scenarios for:
   - Target v1 validation
   - optimization_level 0/1/2 behavior
   - routing strategy determinism and quality expectations
   - scheduling diagnostics and typed errors
2) Add property tests for semantic preservation and determinism under seed/profile.
3) Keep tests intentionally failing where implementation is missing.

Success criteria:
1) Every new user-visible behavior has executable scenario coverage.
2) Determinism and semantic invariants are explicitly asserted.
3) Failure messages are actionable for implementers.

Validation:
1) Run focused failing tests and report expected failures clearly.

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet F1 Prompt - Quantum Kernel Agent (Target + PassManager Core)

```text
You are the Quantum Kernel Agent for NxQuantum (F1: target + pass-manager core).

Objective:
Implement Target v1 and deterministic PassManager level orchestration (0/1/2) behind stable contracts.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/architecture.md
4) docs/adr/0011-compiler-target-contract-v1.md
5) docs/adr/0012-transpiler-pass-manager-levels.md
6) failing tests introduced by Packet E

Allowed write scope:
1) lib/nx_quantum/compiler/target.ex (new)
2) lib/nx_quantum/compiler/pass_manager.ex (new)
3) lib/nx_quantum/compiler/pipeline.ex (new)
4) lib/nx_quantum/compiler.ex
5) minimally required tests touched by this scope

Tasks:
1) Implement target schema/validation and deterministic normalization.
2) Implement pass-manager level contract and stage orchestration.
3) Emit compile report envelope skeleton compatible with plan contracts.
4) Keep behavior deterministic for fixed inputs.

Success criteria:
1) Target and level contract tests pass.
2) No public API regressions.
3) Clear extension points for routing/translation/scheduling packets.

Validation:
1) Run: mise exec -- mix test test/nx_quantum/compiler_test.exs
2) Run targeted new tests related to target/levels.

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet F2 Prompt - Quantum Kernel Agent (Routing Heuristics)

```text
You are the Quantum Kernel Agent for NxQuantum (F2: routing heuristics).

Objective:
Upgrade routing from shortest-path baseline to a SABRE-like deterministic heuristic with explainable diagnostics.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) lib/nx_quantum/transpiler/router.ex
4) lib/nx_quantum/transpiler/report.ex
5) failing routing tests from Packet E

Allowed write scope:
1) lib/nx_quantum/transpiler/router.ex
2) lib/nx_quantum/transpiler/layout.ex (new)
3) lib/nx_quantum/transpiler/cost_model.ex (new)
4) lib/nx_quantum/transpiler/report.ex
5) routing-related tests only

Tasks:
1) Implement deterministic SABRE-like candidate selection with lookahead.
2) Support directed-edge awareness and cost weighting (depth/duration/error profile inputs).
3) Emit route diagnostics:
   - selected candidate
   - rejected alternatives
   - topology pressure indicators
4) Preserve strict typed errors for topology violations.

Success criteria:
1) Deterministic routing for fixed seed/profile/target.
2) Improved SWAP/depth metrics on scoped scenarios.
3) Report contract remains machine-readable and stable.

Validation:
1) Run: mise exec -- mix test test/nx_quantum/transpiler_test.exs
2) Run any new routing benchmark script if created.

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet F3 Prompt - Quantum Kernel Agent (Translation + Optimization)

```text
You are the Quantum Kernel Agent for NxQuantum (F3: translation + optimization).

Objective:
Implement target-aware decomposition and optimization improvements for production compile quality.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) lib/nx_quantum/compiler/pass_pipeline.ex
4) lib/nx_quantum/compiler/passes/*
5) failing optimization tests from Packet E

Allowed write scope:
1) lib/nx_quantum/compiler/pass_pipeline.ex
2) lib/nx_quantum/compiler/passes/*
3) lib/nx_quantum/transpiler/translation.ex (new)
4) optimization-related tests only

Tasks:
1) Add target-aware gate decomposition pipeline.
2) Improve level-aware 1q fusion/cancellation and safe simplifications.
3) Emit deterministic optimization diagnostics (before/after gate/depth/cost).
4) Preserve semantic equivalence within tolerance budgets.

Success criteria:
1) Semantic/property tests pass.
2) Deterministic reports and reduction metrics are reproducible.
3) Integration with pass-manager levels is clean.

Validation:
1) Run: mise exec -- mix test test/nx_quantum/compiler_test.exs
2) Run relevant feature/property suites.

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet F4 Prompt - Quantum Kernel Agent (Scheduling + Cache)

```text
You are the Quantum Kernel Agent for NxQuantum (F4: scheduling + compile cache).

Objective:
Implement scheduling (ASAP/ALAP) and deterministic compile caching with clear diagnostics.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) transpiler/compiler report contracts
4) failing scheduling/cache tests from Packet E

Allowed write scope:
1) lib/nx_quantum/transpiler/scheduler.ex (new)
2) lib/nx_quantum/compiler/cache.ex (new)
3) lib/nx_quantum/transpiler/report.ex
4) scheduling/cache-related tests only

Tasks:
1) Implement ASAP/ALAP scheduling options.
2) Emit scheduling diagnostics (critical path, idle windows, timing summary).
3) Implement deterministic compile cache with explicit hit/miss metadata.
4) Add safe cache key strategy based on circuit+target+profile fingerprint.

Success criteria:
1) Scheduling contract tests pass.
2) Cache determinism and invalidation behavior are tested.
3) Report schema remains stable and machine-readable.

Validation:
1) Run targeted scheduling/cache tests.
2) Run broader compiler/transpiler tests if impacted.

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet G Prompt - Verification Agent (Close-Out)

```text
You are the Verification Agent for NxQuantum (close-out stage).

Objective:
Close milestone verification after implementation packets F1-F4 and ensure deterministic, regression-safe behavior.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) all prior Packet handoffs (E, F1, F2, F3, F4)
4) docs/testing-strategy.md

Allowed write scope:
1) features/*
2) test/features/*
3) test/property/*
4) test/nx_quantum/*
5) test/support/test_support/*

Tasks:
1) Resolve failing-first scenarios and finalize acceptance coverage.
2) Add regression tests for deterministic compile/report outputs.
3) Validate typed error contracts and schema stability.
4) Confirm architecture guard tests still pass.

Success criteria:
1) Feature/property/unit coverage is green for compiler scope.
2) Determinism invariants are enforced.
3) Remaining risks are documented for Docs/Release agents.

Validation:
1) Run: mise exec -- mix test
2) Run: mise exec -- mix test test/property
3) Run: mise exec -- mix test test/features/features_test.exs

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet H Prompt - Docs and Enablement Agent

```text
You are the Docs and Enablement Agent for NxQuantum.

Objective:
Synchronize roadmap/spec/API/README narratives for the new compiler milestone, with clear migration and troubleshooting guidance.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/roadmap.md
4) docs/v0.8-feature-spec.md
5) final verification handoff (Packet G)
6) README.md

Allowed write scope:
1) docs/*.md
2) README.md
3) features/README.md (if needed)

Tasks:
1) Keep docs consistent with implemented capabilities and known gaps.
2) Publish clear operator guidance:
   - choosing optimization levels
   - routing/scheduling strategy tradeoffs
   - interpreting compile diagnostics
3) Update migration/comparison narratives vs Python-first intent.
4) Ensure discoverability from README in under 5 minutes.

Success criteria:
1) No contradictions across docs.
2) Capability/limitation messaging is clear and honest.
3) Links and examples are valid.

Validation:
1) Run: mise exec -- mix docs.build

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Packet I Prompt - Release Agent

```text
You are the Release Agent for NxQuantum.

Objective:
Finalize quality gates, evidence, and release hygiene for the v0.8 compiler milestone.

Required pre-read:
1) AGENTS.md
2) docs/v0.8-compiler-implementation-plan.md
3) docs/roadmap.md
4) final handoffs from Packets G and H
5) docs/release-process.md

Allowed write scope:
1) CI workflows
2) release/changelog artifacts
3) mix.exs (if metadata updates are needed)
4) docs release evidence artifacts

Tasks:
1) Ensure CI includes required compiler milestone gates.
2) Verify correctness + performance + docs evidence is reproducible.
3) Publish release notes with:
   - contract changes
   - migration implications
   - known limits and residual risk

Success criteria:
1) Required release gates pass:
   - mise exec -- mix quality
   - mise exec -- mix dialyzer
   - mise exec -- mix docs.build
2) Evidence bundle is complete and traceable.
3) Release notes are clear and actionable.

Output:
Return the required Handoff template and include absolute file paths in changed files.
```

## Suggested Dispatch Order

1. Packet A
2. Packet B
3. Packet C
4. Packet D
5. Packet E
6. Packet F1
7. Packet F2 + F3 + F4 (parallel after F1 contract merge)
8. Packet G
9. Packet H
10. Packet I

