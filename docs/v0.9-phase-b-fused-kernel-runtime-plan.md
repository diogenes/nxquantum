# v0.9 Phase B Plan - Runtime-Profile Fused Kernel Selection

## Goal

Select fused observable kernels by runtime profile without contract drift, while preserving deterministic numeric equivalence.

## Scope

1. Keep public estimator APIs stable.
2. Route only adapter-internal kernel selection.
3. Preserve fallback behavior for non-eligible term shapes.

## Deliverables

1. Add runtime-profile-aware fused kernel dispatch (`portable` vs `compiled`) for eligible single-wire batches.
2. Keep portable path as deterministic fallback.
3. Ensure runtime profile options propagate through `StateVector -> PauliExpval` integration path.
4. Add guard coverage:
   - `test/nx_quantum/batch_fused_kernel_runtime_profile_guard_test.exs`
   - includes integration-path equivalence checks.

## Strategic Refactoring Pass (DDD/SOLID/Hexagonal)

1. Keep kernel dispatch in adapter internals (`PauliExpval.FusedSingleWire`).
2. Preserve dependency direction and avoid application/domain coupling to kernel implementation details.
3. Reduce duplication in scaling logic and harden runtime-option propagation.

## Status

1. `[x]` Implemented.
2. `[x]` Guard tests added and passing.
