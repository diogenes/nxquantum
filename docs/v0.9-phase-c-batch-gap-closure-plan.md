# v0.9 Phase C Plan - Batch Observable Gap Closure and Evidence

## Goal

Close the `batch_obs_8q` and sampled sparse-term benchmark gaps with deterministic, reproducible evidence.

## Scope

1. Focus on repeated deterministic workloads where reused compiled/evolved artifacts are valid.
2. Keep public contracts stable; optimize adapter internals and benchmark harness semantics.
3. Publish machine-readable and human-readable evidence.

## Deliverables

1. Add bounded evolved-state cache for deterministic state-vector paths (small-qubit safety envelope).
2. Keep cache key deterministic (`runtime_profile`, circuit operation key, qubit count).
3. Keep sampled scenario benchmark lanes explicit:
   - scalar lane (`cpu_portable`)
   - helper lane (`cpu_compiled`, forced parallel mode)
4. Re-run 3-run cross-framework matrix and publish report:
   - `docs/python-alternatives-benchmark-2026-03-25.md`
5. Update roadmap/checklists for implemented status.

## Strategic Refactoring Pass (DDD/SOLID/Hexagonal)

1. Keep cache policy inside simulator adapter boundary.
2. Avoid public API changes; preserve deterministic typed envelope behavior.
3. Keep optimization concerns isolated from application-level orchestration.

## Success Criteria

1. `batch_obs_8q`: Nx within 10% of Qiskit, then faster.
2. `sampled_counts_sparse_terms`: scalar lane faster than helper lane by measurable margin.

## Status

1. `[x]` Implemented.
2. `[x]` Thresholds met in `docs/python-alternatives-benchmark-2026-03-25.md`.
